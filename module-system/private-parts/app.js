// const { even } = require('./utils')

const { multiplyByRandom } = require('./utils');
const utils = require('./utils')

console.log(utils);

const priceList = [1, 4565, 6887, 888, 88]
console.log(utils.multiplyByRandom(priceList, 6))
